export interface Product {
    id: number;
    name: string,
    image: string,
    description: string
    category: string,
    flavor: string,
    price: number,
    mQuantity: number,
    inStock: boolean
}